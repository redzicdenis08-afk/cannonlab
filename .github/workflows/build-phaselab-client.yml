name: Build PhaseLab Client

on:
  push:
    branches:
      - feature/phaselab-client
      - fix/phaselab-passive-telemetry-v4.1
      - fix/phaselab-admin-telemetry-v4.2
    paths:
      - 'phaselab-client/**'
      - '.github/workflows/build-phaselab-client.yml'
  pull_request:
    paths:
      - 'phaselab-client/**'
      - '.github/workflows/build-phaselab-client.yml'
  workflow_dispatch:

concurrency:
  group: phaselab-client-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.1'

      - name: Build client mod
        id: build
        working-directory: phaselab-client
        shell: bash
        run: |
          set -o pipefail
          gradle clean build --stacktrace --console=plain 2>&1 | tee build.log

      - name: Verify passive-only JAR contents
        if: success()
        working-directory: phaselab-client
        shell: bash
        run: |
          JAR='build/libs/phaselab-client-4.2.0.jar'
          test -f "$JAR"
          jar tf "$JAR" | tee jar-contents.txt
          jar tf "$JAR" | grep -F 'dev/denis/phaselab/PhaseTelemetryClient.class'
          jar tf "$JAR" | grep -F 'dev/denis/phaselab/mixin/ClientPacketListenerMixin.class'
          jar tf "$JAR" | grep -F 'phaselab.client.mixins.json'
          jar tf "$JAR" | grep -F 'assets/phaselab/lang/en_us.json'
          ! jar tf "$JAR" | grep -F 'dev/denis/phaselab/PhaseLabClient.class'
          ! jar tf "$JAR" | grep -F 'dev/denis/phaselab/BoatPhaseClient.class'
          unzip -p "$JAR" fabric.mod.json | tee built-fabric.mod.json
          grep -F '"version": "4.2.0"' built-fabric.mod.json
          grep -F '"fabricloader": ">=0.19.2"' built-fabric.mod.json
          grep -F 'dev.denis.phaselab.PhaseTelemetryClient' built-fabric.mod.json
          ! grep -F 'BoatPhaseClient' built-fabric.mod.json
          javap -classpath "$JAR" -p dev.denis.phaselab.PhaseTelemetryClient > telemetry-javap.txt
          grep -F 'onPlayerCorrectionHead' telemetry-javap.txt
          grep -F 'onPlayerCorrectionTail' telemetry-javap.txt
          grep -F 'onVehicleCorrectionHead' telemetry-javap.txt
          grep -F 'onVehicleCorrectionTail' telemetry-javap.txt
          grep -F 'onServerOpenScreen' telemetry-javap.txt
          grep -F 'PHASELAB_LATEST.csv' telemetry-javap.txt
          grep -F 'TEST_START' telemetry-javap.txt
          grep -F 'detail_20hz' telemetry-javap.txt

      - name: Upload build evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phaselab-build-log-v4.2
          path: |
            phaselab-client/build.log
            phaselab-client/jar-contents.txt
            phaselab-client/built-fabric.mod.json
            phaselab-client/telemetry-javap.txt
          if-no-files-found: warn

      - name: Upload JAR
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: phaselab-admin-telemetry-4.2.0-mc1.21.11
          path: phaselab-client/build/libs/phaselab-client-4.2.0.jar
          if-no-files-found: error
