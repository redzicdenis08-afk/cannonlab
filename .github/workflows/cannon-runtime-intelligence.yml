name: Cannon Runtime Intelligence

on:
  pull_request:
    paths:
      - 'src/**'
      - 'build.gradle*'
      - 'settings.gradle*'
      - 'gradle/**'
      - 'gradlew'
      - 'gradlew.bat'
      - 'scripts/cloud-smoke.sh'
      - 'scripts/explain-causal-trace.py'
      - 'scripts/analyze-breach-evidence.py'
      - 'scripts/assert-results.py'
      - 'scenarios/probe-redstone-diagnostic.yml'
      - 'scenarios/breach-telemetry-probe.yml'
      - 'scenarios/probe-unexpected-block-negative.yml'
      - 'cannons/probe-water-protected-dispenser.schem.b64'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  runtime-intelligence:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'

      - name: Compile runtime analysis
        run: python -m py_compile scripts/explain-causal-trace.py

      - name: Build causal recorder
        run: gradle clean build --stacktrace

      - name: Capture ten real redstone-fired causal traces
        env:
          CANNONLAB_MC_VERSION: '26.1.2'
          CANNONLAB_SCENARIO: probe-redstone-diagnostic.yml
          CANNONLAB_EXPECTED_SHOTS: '10'
        run: bash scripts/cloud-smoke.sh

      - name: Verify and explain runtime traces
        run: |
          python - <<'PY'
          import csv
          import json
          import subprocess
          import sys
          from pathlib import Path

          traces = sorted(Path('lab-artifacts/results').rglob('causal-events.csv'))
          assert len(traces) == 10, traces
          for trace in traces:
              with trace.open(newline='', encoding='utf-8') as handle:
                  events = [row['event'] for row in csv.DictReader(handle)]
              assert 'DISPENSE' in events, (trace, events)
              assert 'ENTITY_ADD' in events, (trace, events)
              shot = trace.parent
              subprocess.run([
                  sys.executable,
                  'scripts/explain-causal-trace.py',
                  str(trace),
                  '--json-out', str(shot / 'causal-summary.json'),
                  '--markdown-out', str(shot / 'causal-timeline.md'),
              ], check=True, stdout=subprocess.DEVNULL)
              summary = json.loads((shot / 'causal-summary.json').read_text())
              assert summary['trace_confidence']['requirements']['dispense'], summary
              assert summary['trace_confidence']['requirements']['entity_spawn'], summary
          print(f'Validated and explained {len(traces)} real causal traces.')
          PY

      - name: Upload runtime cannon intelligence evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CannonLab-runtime-intelligence-${{ github.run_number }}
          path: |
            lab-artifacts/**
          if-no-files-found: error
          retention-days: 30

  breach-evidence-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'

      - name: Build breach telemetry recorder
        run: gradle clean build --stacktrace

      - name: Run diagnostic falling-payload overlap probe
        env:
          CANNONLAB_MC_VERSION: '26.1.2'
          CANNONLAB_SCENARIO: breach-telemetry-probe.yml
          CANNONLAB_EXPECTED_SHOTS: '1'
          CANNONLAB_EXPECTED_LIFETIME: none
          CANNONLAB_MIN_EMBEDDED_PAYLOAD_EXPLOSIONS: '1'
          CANNONLAB_MAX_UNEMBEDDED_WATER_EXPLOSIONS: '0'
        run: bash scripts/cloud-smoke.sh

      - name: Verify breach telemetry fail-closed report
        run: |
          python scripts/analyze-breach-evidence.py lab-artifacts/results \
            --min-embedded-payload-explosions 1 \
            --max-unembedded-water-explosions 0 \
            --min-contiguous-layers-before-first-regen 0 \
            --json-out lab-artifacts/breach-evidence.json

      - name: Upload breach telemetry evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CannonLab-breach-evidence-${{ github.run_number }}
          path: lab-artifacts/**
          if-no-files-found: error
          retention-days: 30

  unexpected-block-negative-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'

      - name: Build cumulative integrity recorder
        run: gradle clean build --stacktrace

      - name: Require retained redstone obstruction to fail
        env:
          CANNONLAB_MC_VERSION: '26.1.2'
          CANNONLAB_SCENARIO: probe-unexpected-block-negative.yml
          CANNONLAB_EXPECTED_SHOTS: '1'
          CANNONLAB_EXPECTED_LIFETIME: none
          CANNONLAB_MAX_CANNON_UNEXPECTED_BLOCKS: '0'
        run: |
          set +e
          bash scripts/cloud-smoke.sh
          exit_code=$?
          set -e
          if [[ "$exit_code" -eq 0 ]]; then
            echo 'Negative obstruction scenario unexpectedly passed.' >&2
            exit 1
          fi
          python - <<'PY'
          import json
          from pathlib import Path
          summaries = sorted(
              Path('lab-artifacts/results').rglob('run-summary.json'),
              key=lambda path: path.stat().st_mtime,
              reverse=True,
          )
          assert summaries, 'missing negative run summary'
          summary = json.loads(summaries[0].read_text(encoding='utf-8'))
          assert summary['finish_reason'] == 'contract_failed', summary
          assert len(summary['shots']) == 1, summary
          shot = summary['shots'][0]
          assert shot['contract_pass'] is False, shot
          assert shot['cannon_unexpected_blocks'] >= 1, shot
          assert any(
              failure.startswith('cannon_unexpected=')
              for failure in shot['contract_failures']
          ), shot
          print('Unexpected-block negative runtime failed closed as required.')
          PY

      - name: Upload negative obstruction evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CannonLab-unexpected-block-negative-${{ github.run_number }}
          path: lab-artifacts/**
          if-no-files-found: error
          retention-days: 30
