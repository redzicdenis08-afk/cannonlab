{
  "schemaVersion": 1,
  "id": "mountlab",
  "version": "${version}",
  "name": "MountLab LateDetach",
  "description": "Read-only late detach and correction timeline harness for mounted Fabric 1.21.11 tests.",
  "authors": ["Denis + Ash"],
  "license": "ARR",
  "environment": "client",
  "entrypoints": {
    "client": ["online.coredispatch.mountlab.MountLabClient"]
  },
  "mixins": [
    "mountlab.client.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.2",
    "minecraft": "~1.21.11",
    "java": ">=21",
    "fabric-api": "*"
  }
}
